# .env.example → .env 로 복사해서 사용하세요 (배포 시 반드시 교체)

# 세션/비밀번호 해시용 비밀 키 (길고 랜덤한 값 사용!)
SECRET_KEY=change-me-to-a-long-random-string

# 데이터베이스 URL
#  - 로컬(개발): SQLite (미설정 시 자동)
#  - 배포: 관리형 Postgres 예
# DATABASE_URL=postgresql://user:password@host:5432/dbname
